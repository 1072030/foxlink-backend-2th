# from app.foxlink.model import FoxlinkEvent

# def assemble_device_id(project: str, aux: str, device_name: str) -> str:
#     return f"{project}@{aux}@{device_name}"

# def assemble_device_id_from_event(event: FoxlinkEvent) -> str:
#     project = event.project.split(" ")[0]
#     return assemble_device_id(project,event.line,event.device_name)


    